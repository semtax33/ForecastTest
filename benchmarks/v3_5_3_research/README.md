# V3.5.3 research champion

This directory freezes the grouped-component research benchmark. Oil-heavy and
mixed use the clean component; gas-heavy retains the legacy model. It is not a
production promotion: V3.4 remains the byte-verified production champion and
the live-model lock remains 0/20.

V3.6 macro experiments must verify this manifest before and after execution and
must compare each group against this benchmark without changing it in place.
