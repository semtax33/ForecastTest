[project]
name = "energy-nowcast"
version = "3.4.0"
requires-python = ">=3.11"
dependencies = ["beautifulsoup4", "lxml", "numpy", "pandas", "pyarrow"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
