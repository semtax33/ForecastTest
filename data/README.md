# Local data archive

Root-level legacy CSV exports were moved to `archive/energy_v2`. They are kept
for reproducibility but are not active inputs to the current pipelines. Active
model data belongs in the medallion-organized `../data-lake` directory.

