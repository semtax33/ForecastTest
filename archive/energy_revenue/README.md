# Archived monolithic revenue models

These scripts are historical research records that predate the modular
`energy_nowcast` package. They are preserved for reproducibility and are not
active entrypoints. New model logic belongs in a package module with a thin
entrypoint under `scripts/`.
